# خريطة تطوير المشروع (Future Plans & TODOs)

هذا الملف يحتوي على الأفكار والخطط المستقبلية لتطوير منصة NovaCart التي تم استخراجها:

## 1. تحويل المنصة لدعم المنتجات الرقمية (Digital Products)
- [ ] **تعديل Product Schema**: إضافة حقول `productType` (digital_file, license_key)، `downloadUrl`، و `licenseKeysPool`.
- [ ] **تعديل Order Schema**: إضافة حقول لتخزين بيانات التسليم `deliveredData` (مثل `downloadLink` و `licenseKey`).
- [ ] **تحديث Stripe Webhook**: لتغيير حالة الطلب تلقائياً إلى `Delivered` وتسليم الملفات أو المفاتيح.
- [ ] **تحديث Cart Context**: منع إضافة أكثر من قطعة واحدة للمنتج الرقمي.
- [ ] **لوحة التحكم (Admin)**: إخفاء حقل المخزون وإظهار حقول رفع الملفات وإضافة المفاتيح للمنتجات الرقمية.
- [ ] **إنشاء مسار للتحميل الآمن**: `app/api/download/route.js` للتحقق من الـ Token وإرسال الملف بدون كشف الرابط الحقيقي.

## 2. تحسينات عامة للمتجر (E-commerce Features)
- [ ] **سلة المشتريات (Shopping Cart)**: بناء نظام سلة متكامل بدلاً من الشراء المباشر لقطعة واحدة.
- [ ] **دورة حياة الطلب (Order Fulfillment)**: تحديث حالة الطلبات من لوحة التحكم (Paid -> Shipped -> Delivered).
- [ ] **نظام التقييمات (Reviews & Ratings)**: السماح للعملاء بتقييم المنتجات بعد استلامها.
- [ ] **البحث والفلاتر**: إضافة شريط بحث وفلاتر جانبية في صفحة المنتجات.
- [ ] **إشعارات البريد (Email Receipts)**: دمج `Resend` أو `Nodemailer` لإرسال الفواتير وروابط التحميل للعملاء تلقائياً.

## 3. الذكاء الاصطناعي (AI Features)
- [ ] **المساعد الذكي (AI Shopping Assistant)**: دمج الذكاء الاصطناعي في الشات للرد من قاعدة البيانات.
- [ ] **توليد الوصف التلقائي**: في لوحة التحكم، زر لتوليد وصف تسويقي للمنتجات باستخدام الـ AI.

## 4. مراجعات أمنية وتقنية قبل الإطلاق النهائي
- [ ] التأكد من حذف كود خصم المخزون المزدوج في `verify-session/route.js` (إذا لم يُحذف).
- [ ] تغيير متغير `NEXT_PUBLIC_URL` في الإنتاج ليطابق رابط الموقع الحقيقي.
- [ ] تغيير مفاتيح Stripe إلى مفاتيح الـ Live (إزالة `sk_test`).
- [ ] إعداد Stripe Webhook جديد في بيئة الإنتاج ووضع المفتاح السري الجديد.
